"""Configure the Google Calendar Django application."""

from django.apps import AppConfig


class GooglecalConfig(AppConfig):
    """Identify the Google Calendar app and its default database key type."""

    name = 'googlecal'
